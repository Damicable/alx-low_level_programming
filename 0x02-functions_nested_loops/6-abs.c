#include "main.h"
/**
 * _abs - Write a function that computes the absolute value of an integer.
 * @t: The intiger to be computed
 * Return: the absolute value
 */

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}


